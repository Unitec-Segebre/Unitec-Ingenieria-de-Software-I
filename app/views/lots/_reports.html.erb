<div class="panel panel-default">
  <div class="panel-heading">
    <h4 class="panel-title">
      <a data-toggle="collapse" data-parent="#accordion" href="#<%= reports.id %>" aria-expanded="false" class="collapsed">
        <%= reports.name %>
      </a>
    </h4>
  </div>

  <div id="<%= reports.id %>" class="panel-collapse collapse">
    <div class="panel-body">
      <div class="table-responsive">
        <table class="table">
          <thead>
            <tr>
              <th>Variable</th>
              <th>Valor</th>
              <th>Costo</th>
            </tr>
          </thead>
          <tbody>
            <%= render :partial => "reportVariables", :collection => reports.variables, locals: {lot: lot} %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>