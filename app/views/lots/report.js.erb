$("#charts").html("<% for x in 1..17 %>");
$("#charts").append("<%= j render partial: 'shared/column_chart', locals: {val: x, lot: lot, graph: graph}%>");
$("#charts").append("<% end %>");

$("#accordion").html("<%= j render partial: 'reports', locals: {category: Category.find(1), lot: lot}%>");
$("#accordion").append("<%= j render partial: 'reports_cosecha', locals: {category: Category.find(2), lot: lot}%>");

var checkboxes = "<%= check_box %>"
$(":checkbox").each(function(){
  var tmp = $(this).val();
  if (checkboxes.indexOf(tmp) == -1){
    $(this).attr('checked',false);
  }else{
    var div = $("#chart_div"+tmp)
    div.removeClass("hidden")
  }
});
