<div id="nav-wrapper">
	<div class="row">
		<div class="col-md-10">	
		    <h1 id="title-wrapper">Lotes de <i><%=@project.title %></i></h1>
		    <h4>Empleado encargado: <%=@project.manager%></h4>
		    <%= link_to "Nuevo Lote", new_project_lot_path(@project.id) %> 
		</div>
		<div class="col-md-2">
			<% unless @project.image_url.blank? %>
		      <%=image_tag @project.image_url,:class=>"img_style"%>
		    <%end%>
		</div>
	</div>
</div>

<%=render @project.lots%>
