<div class="panel panel-default">
  <div class="panel-heading">
    <h4 class="panel-title">
      <a data-toggle="collapse" data-parent="#accordion" href="#<%= @category_mantenimiento.id %>" aria-expanded="false" class="collapsed">
        <%= @category_mantenimiento.name %>
      </a>
    </h4>
  </div>

  <div id="<%= @category_mantenimiento.id %>" class="panel-collapse collapse">
    <div class="panel-body">
      <%= form_for [@lot.project, @lot] do |f| %>
        <div class="table-responsive">
          <table class="table">
            <thead>
              <tr>
                <th>Variable</th>
                <th>Cantidad de Hectáreas</th>
                <th>Costo por Mano de Obra</th>
                <th>Costo por Insumo</th>
                <th>Costo por Hectárea por Mano de Obra</th>
                <th>Costo por Hectárea por Insumo</th>
                <th>Costo por Hectárea</th>
                <th>Costo Total</th>
              </tr>
            </thead>
            <tbody>
              <%= render :partial => "variable", :collection => @category_mantenimiento.variables, locals: {lot: lot} %>
            </tbody>
          </table>
        </div>
        <%= f.submit 'Guardar', class: 'btn btn-default pull-right' %>
      <% end %>
    </div>
  </div>
</div>
