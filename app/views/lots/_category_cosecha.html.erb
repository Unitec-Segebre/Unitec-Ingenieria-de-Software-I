<div class="panel panel-default">
  <div class="panel-heading">
    <h4 class="panel-title">
      <a data-toggle="collapse" data-parent="#accordion" href="#<%= category.id %>" aria-expanded="false" class="collapsed">
        <%= category.name %>
      </a>
    </h4>
  </div>

  <div id="<%= category.id %>" class="panel-collapse collapse in">
    <div class="panel-body">
      <% lot = Lot.find(lot_id) %>
      <%= form_for [lot.project, lot] do |f| %>
        <div class="table-responsive">
          <table class="table">
            <thead>
              <tr>
                <th>Variable</th>
                <th>HAS Recorridas</th>
                <th>T.M.FF</th>
                <th>Costo Cosecha</th>
                <th>Racimos Cosechados</th>
                <th>Sacos Recolectados</th>
                <th>Costo/Toneladas</th>
                <th>Racimos/HAS Totales</th>
                <th>Racimos/Plantas</th>
                <th>Sacos Recolectados/HAS Recorridas</th>
                <th>Peso Racimos</th>
              </tr>
            </thead>
            <tbody>
              <%= render partial: "variables_cosecha", collection: category.variables, locals: {lot: lot, date: date} %>
            </tbody>
          </table>
        </div>
        <%= f.hidden_field :date, value: "", id: "hidden_date_cosecha" %>
        <%= f.hidden_field :category, value: "Cosecha" %>
        <%= f.submit 'Guardar', class: 'btn btn-default pull-right' %>
      <% end %>
    </div>
  </div>
</div>

<script>
  $('#hidden_date_cosecha').val($('#date').val());
</script>
