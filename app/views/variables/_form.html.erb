<div role="document">
  <div class="modal-header">
      <h3>Nueva Variable</h3>
  </div>
  <div class="modal-body">
    <%= form_for @variable do |c| %>
      <div class="form-group">    
        <%= c.label :name, "Nombre" %>
        <%= c.text_field :name, id: "field", class: "form-control" %>
      </div>
      <div class="form-group">    
        <%= c.label :category_id, "Categoría" %>
        <%= c.text_field :category_id, id: "field", class: "form-control" %><!-- cambiar a dropdown de modelo categories -->
      </div>
      <div class="form-group">    
        <%= c.label :measurement_unit, "Unidad" %>
        <%= c.text_field :measurement_unit, id: "field", class: "form-control" %><!-- cargar de dropdown list -->
      </div>
      <div class="form-group">    
        <%= c.label :unit_cost, "Costo" %>
        <%= c.text_field :unit_cost, id: "field", class: "form-control" %><!-- hacer un formato de lps, talves con un up y down -->
      </div>
  </div>
  <div class="modal-footer">
    <div class="btn-group" role="group">
        <button type="button" class="btn btn-danger" data-dismiss="modal" onClick="clear_field()">Cancelar</button>
      <%= c.submit "Guardar",class: "btn btn-primary" %>
    </div>
  </div>
  <%end%>
</div>

<script type="text/javascript">
function clear_field(){
  $("#field").val("");
}
</script>