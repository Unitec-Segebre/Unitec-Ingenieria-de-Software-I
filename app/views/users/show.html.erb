<h1 class="page-header">Información del Usuario</h1>
<div class="row">
    <div class="col-lg-12">
        <div class="panel">
            <div class="panel-body">
                <div class="table-responsive">
                    <table width="100%" class="table">
                        <thead>
                            <tr>
                                <th></th>
                                <th>Usuario</th>
                                <th>Primer Nombre</th>
                                <th>Segundo Nombre</th>
                                <th>Apellido</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td><%=link_to(image_tag(@user.get_image,:class=>"img_style"),@user) %></td>
                                <td><%= @user.email%></td>
                                <td><%= @user.first_name%></td>
                                <td><%= @user.middle_name%></td>
                                <td><%= @user.last_name%></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
                <!-- /.table-responsive -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
</div>
